// THIS FILE IS AUTO-GENERATED BY ZWGEN
// DO NOT MODIFY

package networkmanagementproxyv2

import (
	"encoding/gob"
	"errors"

	"github.com/gozwave/gozw/cc"
)

const CommandNmMultiChannelEndPointReport cc.CommandID = 0x06

func init() {
	gob.Register(NmMultiChannelEndPointReport{})
	cc.Register(cc.CommandIdentifier{
		CommandClass: cc.CommandClassID(0x52),
		Command:      cc.CommandID(0x06),
		Version:      2,
	}, NewNmMultiChannelEndPointReport)
}

func NewNmMultiChannelEndPointReport() cc.Command {
	return &NmMultiChannelEndPointReport{}
}

// <no value>
type NmMultiChannelEndPointReport struct {
	SeqNo byte

	Nodeid byte

	Properties1 struct {
		IndividualEndPoints byte

		Res1 bool
	}

	Properties2 struct {
		AggregatedEndPoints byte

		Res2 bool
	}
}

func (cmd NmMultiChannelEndPointReport) CommandClassID() cc.CommandClassID {
	return 0x52
}

func (cmd NmMultiChannelEndPointReport) CommandID() cc.CommandID {
	return CommandNmMultiChannelEndPointReport
}

func (cmd NmMultiChannelEndPointReport) CommandIDString() string {
	return "NM_MULTI_CHANNEL_END_POINT_REPORT"
}

func (cmd *NmMultiChannelEndPointReport) UnmarshalBinary(data []byte) error {
	// According to the docs, we must copy data if we wish to retain it after returning

	payload := make([]byte, len(data))
	copy(payload, data)

	if len(payload) < 2 {
		return errors.New("Payload length underflow")
	}

	i := 2

	if len(payload) <= i {
		return errors.New("slice index out of bounds")
	}

	cmd.SeqNo = payload[i]
	i++

	if len(payload) <= i {
		return errors.New("slice index out of bounds")
	}

	cmd.Nodeid = payload[i]
	i++

	if len(payload) <= i {
		return errors.New("slice index out of bounds")
	}

	i++ // skipping over reserved bit

	if len(payload) <= i {
		return errors.New("slice index out of bounds")
	}

	cmd.Properties1.IndividualEndPoints = (payload[i] & 0x7F)

	cmd.Properties1.Res1 = payload[i]&0x80 == 0x80

	i += 1

	if len(payload) <= i {
		return errors.New("slice index out of bounds")
	}

	cmd.Properties2.AggregatedEndPoints = (payload[i] & 0x7F)

	cmd.Properties2.Res2 = payload[i]&0x80 == 0x80

	i += 1

	return nil
}

func (cmd *NmMultiChannelEndPointReport) MarshalBinary() (payload []byte, err error) {
	payload = make([]byte, 2)
	payload[0] = byte(cmd.CommandClassID())
	payload[1] = byte(cmd.CommandID())

	payload = append(payload, cmd.SeqNo)

	payload = append(payload, cmd.Nodeid)

	{
		var val byte

		val |= (cmd.Properties1.IndividualEndPoints) & byte(0x7F)

		if cmd.Properties1.Res1 {
			val |= byte(0x80) // flip bits on
		} else {
			val &= ^byte(0x80) // flip bits off
		}

		payload = append(payload, val)
	}

	{
		var val byte

		val |= (cmd.Properties2.AggregatedEndPoints) & byte(0x7F)

		if cmd.Properties2.Res2 {
			val |= byte(0x80) // flip bits on
		} else {
			val &= ^byte(0x80) // flip bits off
		}

		payload = append(payload, val)
	}

	return
}
